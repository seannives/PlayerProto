//Copyright 2012, etc.

(function (root, factory) {
    if (typeof define === 'function' && define.amd) {
        // AMD.
        define(['jquery', 'underscore', 'd3'], factory);
    } else {
        // Browser globals
        root.interactives = factory(root.$, root._, root.d3);
    }
}(this, function ($, _, d3) {


